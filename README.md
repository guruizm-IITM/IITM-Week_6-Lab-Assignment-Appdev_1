# Flask REST API ✅

This repository contains a **single-file Flask REST API** implementation that successfully passed all test cases.  
It demonstrates how to build a production-ready API using **Flask**, **Flask-RESTful**, and **SQLAlchemy** in a clean, modular, and minimal structure.

---

## 🚀 Features
- Built entirely in one file (`app.py`)
- RESTful endpoints for CRUD operations
- SQLAlchemy ORM with SQLite database
- Error handling for invalid routes and data
- Clean code passing all automated tests

---

## 🧩 Tech Stack
- **Python 3.10+**
- **Flask**
- **Flask-RESTful**
- **Flask-SQLAlchemy**

---

## ⚙️ Setup Instructions

1. Clone the repository:
   ```bash
   git clone https://github.com/<your-username>/<repo-name>.git
   cd <repo-name>
   ```

2. Create and activate a virtual environment:
   ```bash
   python -m venv venv
   source venv/bin/activate     # macOS/Linux
   venv\Scripts\activate      # Windows
   ```

3. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

4. Run the application:
   ```bash
   python main.py
   ```

5. Access the API at:
   ```
   http://127.0.0.1:5000/
   ```

---

## 🧪 Running Tests
Run automated tests (if provided) using:
```bash
pytest
```

---

## 📂 Project Structure
```
├── app.py              # Flask REST API (single file)
├── requirements.txt     # Dependencies
└── README.md            # Project documentation
```

---

## 🏆 Status
✅ All test cases passed successfully.

---

## 📜 License
This project is released under the [MIT License](LICENSE).
